import BottomContainer from "./ui/maple/BottomContainer";
import TopContainer from "./ui/maple/TopContainer";

export default function Home() {
  return (
    <main className="flex flex-col w-full">
      <TopContainer />
      <BottomContainer />
    </main>
  );
}

